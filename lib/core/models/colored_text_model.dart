import 'package:flutter/material.dart';

class ColoredText {
  final String? text;
  final Color? color;
  final bool? isBold;

  ColoredText({@required this.text, @required this.color, this.isBold});
}